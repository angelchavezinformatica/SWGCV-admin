const baseURL: string = "http://127.0.0.1:8000/";
const baseAPI: string = baseURL + "api/";

export const LoginUrl: string = baseAPI + "auth/login";
export const AuthUrl: string = baseAPI + "auth/auth";

export const AdminInventory: string = baseAPI + "inventory/admin";

export const AllInventory: string = baseAPI + "inventory/all";
export const AllProvider: string = baseAPI + "provider/all";
